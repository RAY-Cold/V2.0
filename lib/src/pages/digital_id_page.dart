import 'package:flutter/material.dart';
class DigitalIDPage extends StatelessWidget {
  const DigitalIDPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Digital ID')), body: const Center(child: Text('Digital ID Page')));
}
