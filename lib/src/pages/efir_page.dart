import 'package:flutter/material.dart';
class EFIRPage extends StatelessWidget {
  const EFIRPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('eFIR')), body: const Center(child: Text('eFIR Page')));
}
