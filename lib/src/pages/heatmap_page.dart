import 'package:flutter/material.dart';
class HeatmapPage extends StatelessWidget {
  const HeatmapPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Heatmap')), body: const Center(child: Text('Heatmap Page')));
}
